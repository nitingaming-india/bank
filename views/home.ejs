<!-- views/home.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Banking System - Home</title>
  <style>
    /* General Styles */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #1a1a1a;
      color: #ffffff;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    /* Container for the content */
    .home-container {
      text-align: center;
      background-color: #2a2a2a;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
      max-width: 400px;
      width: 100%;
    }

    /* Title */
    .home-container h1 {
      font-size: 2.5rem;
      margin-bottom: 20px;
      color: #00ff88;
      animation: fadeIn 2s ease-in-out;
    }

    /* Description */
    .home-container p {
      font-size: 1.1rem;
      margin-bottom: 30px;
      color: #cccccc;
      animation: fadeIn 3s ease-in-out;
    }

    /* Role Selection Buttons */
    .role-buttons {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .role-buttons a {
      text-decoration: none;
      color: #ffffff;
      background-color: #00ff88;
      padding: 12px 20px;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: bold;
      transition: background-color 0.3s ease, transform 0.3s ease;
    }

    .role-buttons a:hover {
      background-color: #00cc66;
      transform: translateY(-3px);
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
  </style>
</head>
<body>
  <div class="home-container">
    <h1>Welcome to NovaBank</h1>
    <p>Your trusted partner in modern banking. Please choose your role to log in:</p>
    <div class="role-buttons">
      <a href="/login?role=admin">Login as Admin</a>
      <a href="/login?role=employee">Login as Employee</a>
    </div>
  </div>
</body>
</html>